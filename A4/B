这一题有三个坑：
1、remove耗时过长，需要用标记法删除然后在poll的时候把被标记的节点去掉
2、溢出问题。不只是最后的结果输出需要用到long，想象一下当两个负数（都取最小值）相加，再减去一个正数，而这个正数恰好又比较大时，会向下溢出，负数转正数，导致结果错误。
所以节点的value也需要使用long。
3、[at most k]审题，不是必须要有K个元素，是最多K个。当大顶堆开始吐出负数时，break。
